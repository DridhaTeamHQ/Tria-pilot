='src/lib/tryon/production-tryon-pipeline.ts'
=Get-Content 
=[System.Collections.Generic.List[string]]::new()
foreach( in ){
    .Add()
    if( -eq '    const sessionId = prod--'){
        .Add('    const finalRenderEngine = ''nano-banana-pro''')
        .Add('    const skipPixelOverwrite = finalRenderEngine === ''nano-banana-pro''')
    }
}
Set-Content -Path  -Value  -Encoding utf8
